<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title><%= title %></title>
<link rel="stylesheet" href="form.css">
</head>
<body>

<div class="container">
    <h2>Запись к врачу</h2>
    <form id="contact-form" method="post">
        <label for="full-name">ФИО:</label>
        <input type="text" id="full-name" name="full-name" placeholder="Введите ФИО" required>

        <label for="phone-number">Номер телефона:</label>
        <input type="tel" id="phone-number" name="phone-number" placeholder="Введите номер телефона" required>

        <label for="problem-description">Опишите вашу проблему:</label>
        <textarea id="problem-description" name="problem-description" ></textarea >
        <div class="submit">
            <% if (success) {%> 
            <button type="submit" id="submit-button" data-type="send" disabled>Отправить</button>
            <% } else { %>
            <button type="submit" id="submit-button" data-type="send">Отправить</button>
            <% } %>
        </div>
        
    </form>
    <% if (success) {%> 
    <div id="successMessage" class="success-message">Сообщение успешно отправлено!</div>
    <% } %>
    <% if (error) {%>
    <div id="errorMessage" class="error-message"><%= error %></div>
    <% } %>
</div>

</body>
</html>
